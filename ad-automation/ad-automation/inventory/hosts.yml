---
all:
  children:
    dc:
      hosts:
        dc01:
          ansible_host: 192.0.2.10        # <-- your domain controller's IP/DNS
    windows_clients:
      hosts: {}                            # add machines to domain-join here, e.g.
        # client01:
        #   ansible_host: 192.0.2.50
